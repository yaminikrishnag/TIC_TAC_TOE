package tictactoe;
//This enum serves both purpose as move and also as the user.
// ASSUMPTION is that Human player always takes X and computer always takes O
public enum Moves {
	 EMPTY, CROSS, NOUGHT
}
